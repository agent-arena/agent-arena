# Railway configuration for Agent Arena
[build]
builder = "dockerfile"

[deploy]
startCommand = "python -m uvicorn arena.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
