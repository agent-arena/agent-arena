# Railway configuration for Agent Arena
[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c 'python -m uvicorn arena.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
